spring:
  application:
    name: count10shop
  profiles:
    default: release

management:
  endpoint:
    health:
      show-details: when_authorized
  endpoints:
    jmx:
      exposure:
        include: health