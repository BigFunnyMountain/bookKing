spring:
  application:
    name: count10shop
  profiles:
    default: dev

management:
  endpoint:
    health:
      show-details: when_authorized
  endpoints:
    jmx:
      exposure:
        include: health